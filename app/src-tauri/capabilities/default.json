{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "FaceCam default capabilities",
  "windows": ["main", "region-selector"],
  "permissions": ["core:default", "fs:default", "core:window:allow-close"]
}

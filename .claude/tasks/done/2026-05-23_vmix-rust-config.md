# T1 + T2: Rust — vMix Integration

**Status:** in_progress  
**Objective:** Replace ServerConfig/Socket.io with VmixConfig + direct HTTP vMix API calls.  
**Files:** `app/src-tauri/src/lib.rs`, `app/src-tauri/Cargo.toml`

{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "FaceCam Observer capabilities",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "fs:default",
    "dialog:default",
    "core:window:allow-close"
  ]
}
